use crate::docker::{Docker, DockerContainerId, DockerError};

impl Docker {
    pub fn upgrade_service(project_name: String, service_name: String) -> Result<String, DockerError> {
        todo!()
    }
}